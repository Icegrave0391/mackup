[user]
	name = Icegrave0391
	email = chuqiz@comp.nus.edu.sg
[core]
	editor = nvim
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
